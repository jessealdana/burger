<body>

<h1>The Burger Compendium</h1>
<hr>

<div class="container">
  <div class="row">
    <div class="col-sm-6">
      <h2>Burgers that are delicious.</h2>
      <ul>
        {{#each burgers}}
        {{#unless devoured}}
          {{> burgers/burger-block devoured=true}}
        {{/unless}}
        {{/each}}
      </ul>
    </div>

    <div class="col-sm-6">
      <h2>Burgers that were delicious.</h2>
      <ul>
        {{#each burgers}}
        {{#if devoured}}
          {{> burgers/burger-block devoured=false}}
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
</div>

<hr>

<div class="container2">
  <div class="row">
    <div class="col-sm-12">
<h3>Give the gift of a Burger</h3>
<br>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devo">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Uneaten
  </div>

  <button type="submit">Add Burger</button>
</form>
    </div>
  </div>
</div>

</body>